<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />

    <title><%= content_for?(:title) ? yield(:title) : "My Blog" %></title>

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="min-h-screen bg-gray-50 text-gray-900 antialiased">
    <% unless controller_name == "posts" && action_name == "show" %>
      <header class="container mx-auto my-8">
        <% if controller_name == "home" && action_name == "index" %>
          <h1 class="text-5xl font-bold text-center">Jeremy Gates</h1>
          <h2 class="text-2xl font-semibold text-center mt-2">Posts</h2>
        <% elsif controller_name == "posts" && action_name == "index" %>
          <h1 class="text-4xl font-bold text-center">Posts</h1>
        <% else %>
          <% if content_for?(:title) %>
            <h1 class="text-4xl font-bold text-center"><%= yield :title %></h1>
          <% else %>
            <h1 class="text-4xl font-bold text-center">Posts</h1>
          <% end %>
        <% end %>
      </header>
    <% end %>

    <div class="container mx-auto px-4">
      <% flash.each do |type, message| %>
        <div class="mb-4 rounded-lg border px-4 py-3 text-sm
          <%= type.to_s == 'alert' ? 'border-red-300 bg-red-50 text-red-800' : 'border-green-300 bg-green-50 text-green-800' %>">
      <%= message %>
    </div>
  <% end %>

  <main>
    <%= yield %>
  </main>
    </div>
  </body>
</html>
